{{ knp_pagination_sortable(articles, 'Date', 'a.createdAt', {'direction': 'DESC', 'class': 'btn btn-primary d-flex gap-2 flex-row-reverse' }) }}


{{ knp_pagination_sortable(articles, 'Titre', 'a.titre', {'direction': 'ASC', 'class': 'btn btn-primary d-flex gap-2 flex-row-reverse' }) }}
