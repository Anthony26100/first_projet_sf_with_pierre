include:
  - UserFixtures.yaml
  - TagFixtures.yaml

App\Entity\Article:
  article{1..19}:
    titre (unique): "Article <current()>"
    content: <generateContent()>
    user: "<randomElement(@user{1..10})>"
    categories: "<randomElements(@category{1..10})>"
    createdAt: "<generateDate()>"
    active: true

  articleName:
    titre (unique): "Article de test"
    content: <generateContent()>
    user: "<randomElement(@user{1..10})>"
    categories: "<randomElements(@category{1..10})>"
    createdAt: "<generateDate()>"
    active: true
